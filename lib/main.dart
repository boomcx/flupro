import 'package:flutter/material.dart';

void main() {
  runApp(const FluproApp());
}

class FluproApp extends StatelessWidget {
  const FluproApp({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
